interface:
  display_name: "Luma Monitor"
  short_description: "Repo-specific patterns and workflows for luma-monitor"
  default_prompt: "Use the luma-monitor repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true