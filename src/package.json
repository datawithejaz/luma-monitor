{
  "name": "luma-monitor",
  "version": "1.0.0",
  "description": "Lu.ma London event monitor — notifies via email when new relevant events appear",
  "main": "monitor.js",
  "scripts": {
    "start": "node monitor.js",
    "sync-calendars": "node sync-tracked-calendars.js",
    "probe-auth": "node probe_auth.js",
    "test": "node --test"
  },
  "dependencies": {
    "nodemailer": "^9.0.1"
  },
  "engines": {
    "node": ">=18"
  }
}
