# Node
node_modules/
npm-debug.log*

# Local env / secrets
.env

# Personal calendar exports (contain your RSVPs — never commit to a public repo)
*.ics

# Archived prototype (kept locally, not published)
_archive/

# OS
.DS_Store
Thumbs.db

# Note: src/seen_events.json, src/event_meta.json, src/known_calendars.json,
# src/sitemap_crawl.json, src/calendar_digest.json and src/tracked_calendars.json
# are intentionally tracked — monitor state/config.
