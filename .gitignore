# Node
node_modules/
npm-debug.log*

# Local env / secrets
.env

# Archived prototype (kept locally, not published)
_archive/

# OS
.DS_Store
Thumbs.db

# Note: src/seen_events.json, src/event_meta.json, src/known_calendars.json,
# src/tracked_calendars.json, and src/sitemap_checked.json are intentionally
# tracked — monitor state/config.
